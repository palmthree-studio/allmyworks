import { AdjustTextSizeDirective } from './adjust-text-size.directive';

describe('AdjustTextSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new AdjustTextSizeDirective();
    expect(directive).toBeTruthy();
  });
});
